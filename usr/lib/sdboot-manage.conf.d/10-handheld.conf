#!/bin/bash

SYS_ID="$(cat /sys/devices/virtual/dmi/id/product_name)"

# Steam Deck OLED and LCD
if [[ ":Jupiter:Galileo:" =~ ":$SYS_ID:" ]]; then
    LINUX_OPTIONS+=" amd_iommu=off amdgpu.ppfeaturemask=0xffffffff"
fi

unset SYS_ID
